#include <windows.h>
#include <iostream>

using namespace std;
int main(){
/*
// program for get ascii representation for any character and vice versa
    char c;
    cout << "Enter character : ";
    cin >> c;
    cout << (int)c << endl;
    int ascii;
    cout << "Enter Ascii representation number : ";
    cin >> ascii;
    cout << (char) ascii << endl;

// program for divide two numbers
    int x, y;
    cout << "Enter first num : ";
    cin >> x;
    cout << "Enter second num : ";
    cin >> y;
    float z = (float) x/y;
    cout << z;


// revision on conditions and loops and arrays
    // if condition
    unsigned int grade;
    cout << "Enter your grade : ";
    cin >> grade;
    if(grade < 50){
        cout << "F" << endl;
    }else if (grade < 70){
        cout << "D" << endl;
    }else if(grade < 80){
        cout << "C" << endl;
    }else if(grade < 90){
        cout << "B" << endl;
    }else if(grade < 100){
        cout << "A" << endl;
    }else {
        cout << "Please enter right grade." << endl;
    }

    // switch case
    int day;
    cout << "Enter the day number from 1 to 7 : ";
    cin >> day;
    switch(day){
        case 1: cout << "Sat." << endl;
        break;
        case 2: cout << "Sun." << endl;
        break;
        case 3: cout << "Mon." << endl;
        break;
        case 4: cout << "Tue." << endl;
        break;
        case 5: cout << "Wed." << endl;
        break;
        case 6: cout << "Thu." << endl;
        break;
        case 7: cout << "Fri." << endl;
        break;
        default: cout << "Please enter number between 1 and 7." << endl;
    }
*/
/*
    // loops
    int i = 1;
    while(i <= 10){
        cout << i << endl;
        i++;
    }
    cout << "######################" << endl;
    do{
        cout << i << endl;
        i++;
    }while(i <= 20);
    cout << "######################" << endl;

    for (int i=0; i < 10; i++){
        for(int k=10; k>i; k--){
                cout << " ";
        }
        for(int j=0; j<i*2; j++){
            cout << "*";
        }
        cout << endl;
    }
    cout << endl;
    // Pointers and References
    string name = "ayook";
    string &sameName = name; //(two variables refers to "ayook" in memory) name and sameName
    string* ptr = &name; // save memory address of name in ptr variable
    cout << "Reference of " << name << " is : " << &name << endl;
    cout << sameName << endl;
    cout << &sameName << endl; // memory address of name
    cout << ptr << endl; // contains the memory address of name
    cout << &ptr << endl;
    */
/*
    int arrSize;
    cin >> arrSize;
    int arr[arrSize];
    for(int i=0;i<arrSize;i++){
        cin>>arr[i];
    }
    for(int i=arrSize-1;i>=0;i--){
        cout<<"arr["<<i<<"] : "<<arr[i]<<endl;
    }
  */

    return 0;
}
